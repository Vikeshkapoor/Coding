#include<stdio.h>
int main()
{//typecasting is used for changing data type
    int a=100;
    int b=9;
    float c=(float)a/b;//here i used (float) so that my answer will be in float 
    //or rather than doing this if i will use float for a and b then the answer will be also in float
    printf("%f",c);
    return 0;
}
 